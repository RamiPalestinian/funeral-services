# Оценка проекта «Пантеон» (funeral-services)

Документ описывает объём кодовой базы, функциональность и ориентировочную стоимость разработки / продажи продукта.  
Дата оценки: май 2026.

---

## Что внутри (краткий аудит)

| Параметр | Значение |
|----------|----------|
| Исходники (client + server, без тестов) | **~128 файлов, ~7 750 строк** TS/TSX |
| Стили | **39 CSS-файлов** (своя вёрстка, без UI-kit) |
| Фронт | **Next.js 16**, React 19, **Redux Toolkit**, React Hook Form, **Zod** |
| Бэк | **NestJS 11**, **PostgreSQL**, Sequelize, **JWT** (access + refresh) |
| Страницы | ~20 маршрутов: главная, каталоги (классика / ислам / кремация / магазин), карточки, корзина, checkout, профиль, auth, AI-чат, контакты |
| API | Auth, users, 4 каталога (CRUD), корзина, **mock-checkout**, **GigaChat** |
| БД | 6 миграций, сидеры |
| Тесты | 16 `*.spec.ts` на сервере (в основном каркас Nest) |
| Архитектура | **FSD** (entities / features / widgets / shared) — выше среднего для буткемп-проекта |

### Реализовано

- Регистрация / логин, профиль (аватар, пароль, удаление)
- Каталоги с детальными страницами: классические похороны, исламские, кремация, ритуальный магазин
- Корзина, оформление заказа с выбором способа оплаты (карта / СБП QR / наличные)
- **Имитация оплаты** (mock-checkout)
- AI-консультант (GigaChat) с промптом под ритуальные услуги
- Toast-уведомления, confirm-dialog
- «Админка» через `user?.id === 1` на фронте

### Не реализовано (для «боевого» продукта)

- Реальная оплата (ЮKassa и т.п.)
- Роли и защита CRUD на API (POST/PATCH/DELETE каталогов **без guard**)
- Docker / CI/CD
- Полноценный README для развёртывания
- Заказы, уведомления, email
- Полноценная админ-панель
- Продакшен-деплой, мониторинг

---

## Сколько стоило бы разработать с нуля

Оценка трудозатрат для команды уровня middle (один fullstack или 2 человека):

| Блок | Часы (ориентир) |
|------|-----------------|
| UI/вёрстка + ~20 страниц | 80–120 |
| Redux, формы, валидация, интеграция с API | 40–60 |
| NestJS: auth, 5 модулей, Sequelize, миграции | 60–80 |
| Корзина + checkout (mock) | 20–30 |
| GigaChat-чат | 10–15 |
| Базовые тесты + отладка | 15–25 |
| **Итого** | **~225–330 ч** → усреднённо **~260 ч** |

### Стоимость разработки «под ключ» (РФ)

| Кто делает | Ставка | Итого за ~260 ч |
|------------|--------|-----------------|
| Студент / джун после курса | 800–1 500 ₽/ч | **200 000 – 400 000 ₽** |
| Фрилансер middle | 2 000–3 500 ₽/ч | **520 000 – 900 000 ₽** |
| Небольшая студия | 3 500–5 000 ₽/ч | **900 000 – 1 300 000 ₽** |
| Западный фриланс ($60–90/ч) | — | **$15 000 – 24 000** (~1,4–2,2 млн ₽) |

Для **буткемп-финального проекта** по объёму и качеству это уровень **сильного выпускного**: не «игрушка на выходные», но и не коммерческий MVP «под ключ».

---

## Сколько можно «продать» как готовый продукт

Здесь цена ниже, чем стоимость разработки — покупатель платит за готовность к бизнесу, а не за строки кода.

| Сценарий | Реалистичный диапазон (РФ) |
|----------|----------------------------|
| Исходники «как есть» (портфолио, доработка заказчиком) | **50 000 – 150 000 ₽** |
| MVP для ритуального агентства (каталог + корзина + AI, без реальной оплаты) | **150 000 – 400 000 ₽** |
| После доработки: ЮKassa, RBAC, защищённый API, деплой, админка | **+400 000 – 800 000 ₽** к текущему состоянию |

На западном рынке такой MVP без compliance и реальных платежей обычно **$3 000 – $8 000** как white-label, если есть дизайн и демо.

---

## Итог одной цифрой

| Вопрос | Ответ |
|--------|--------|
| **Сколько стоило бы заказать такое с нуля в РФ?** | **~500 000 – 900 000 ₽** (middle, без отдельного дизайнера и DevOps) |
| **Сколько стоит сейчас «как есть»?** | **~100 000 – 300 000 ₽** как рабочий прототип / старт для бизнеса |
| **Сколько стоит «как продукт в проде»?** | **~800 000 – 1 500 000 ₽** с оплатой, безопасностью и деплоем |

---

## Что сильнее всего влияет на цену

### Плюсы (дороже)

- Полный стек (Next.js + NestJS + PostgreSQL)
- Архитектура FSD
- 4 каталога услуг / товаров
- Auth с refresh-токенами
- AI-консультант (GigaChat)
- Аккуратный UX под нишу
- Существенный объём кода (~7 750 строк)

### Минусы (дешевле)

- Mock-оплата вместо платёжного шлюза
- Админ по `user?.id === 1` без ролей на бэкенде
- Открытые мутации API каталогов
- Нет прод-инфраструктуры (Docker, CI, деплой)

Для инвестора или заказчика это скорее **«демо + база для доработки»**, чем готовый коммерческий продукт.

---

## Оценка по модулям (для защиты / КП)

| Модуль | Часы | Стоимость (2 500 ₽/ч) |
|--------|------|------------------------|
| Auth + профиль | 35–45 | 87 500 – 112 500 ₽ |
| Каталоги (4 типа) + CRUD | 70–90 | 175 000 – 225 000 ₽ |
| Корзина + checkout | 25–35 | 62 500 – 87 500 ₽ |
| UI / вёрстка | 80–120 | 200 000 – 300 000 ₽ |
| AI-чат (GigaChat) | 10–15 | 25 000 – 37 500 ₽ |
| БД, миграции, сидеры | 10–15 | 25 000 – 37 500 ₽ |
| **Итого** | **~230–320** | **~575 000 – 800 000 ₽** |

---

## Стек и структура репозитория

```
funeral-services/
├── client/     # Next.js 16, React 19, Redux Toolkit
└── server/     # NestJS 11, Sequelize, PostgreSQL
```

Основные зависимости клиента: `@reduxjs/toolkit`, `axios`, `react-hook-form`, `zod`, `react-markdown`.  
Основные зависимости сервера: `@nestjs/jwt`, `passport-jwt`, `bcrypt`, `sequelize`, `gigachat`.

---

*Оценка ориентировочная и зависит от региона, срочности, наличия дизайна и требований заказчика.*
